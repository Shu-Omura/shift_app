<header>
  <nav class="navbar navbar-fixed-top">
    <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#nav_target">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to 'ShiftAPP', root_path, class: "navbar-brand" %>
      </div>
      <div class="navbar-collapse collapse" id="navbar-target">
        <ul class="nav navbar-nav navbar-right">
          <li class="dropdown">
            <a class="dropdown-toggle" href="#" data-toggle="dropdown">
              <%= user_signed_in? ? current_user.name : 'ようこそゲストさん。' %>
            <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <% if user_signed_in? %>
                <li><%= link_to 'マイアカウント', current_user_path %></li>
                <li class="divider"></li>
                <li><%= link_to 'ログアウト', '#' %></li>
              <% else %>
                <li><%= link_to 'サインイン', new_user_registration_path %></li>
                <li><%= link_to 'ログイン', new_user_session_path %></li>
              <% end %>
            </ul>
          </li>
        </ul>
      </div>
    </div>
  </nav>
</header>